<Window x:Class="Library.Views.ReportsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="Отчёты" Height="500" Width="700"
        WindowStartupLocation="CenterOwner"
        Background="#F5F0EB">

    <Border Margin="20" Background="White" CornerRadius="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Количество книг по жанрам" FontSize="22" FontWeight="Bold" Foreground="#2C3E50" HorizontalAlignment="Center" Margin="0,15"/>

            <lvc:PieChart Grid.Row="1" x:Name="GenreChart" 
                      LegendLocation="Right"
                      InnerRadius="0"
                      Width="400" Height="350"
                      HorizontalAlignment="Center"/>
        </Grid>
    </Border>
</Window>